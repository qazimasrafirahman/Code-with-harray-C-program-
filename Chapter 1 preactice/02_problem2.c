#include <stdio.h>

int main()
{
    // int radius = 7;
    // float pi = 3.1417, area;

    // area = pi * radius * radius;

    // printf("Area of circle: %.2f",area );

    float volume, r = 3, h = 10;
    volume = 3.1416 * r * h;

    printf("Volume of a cylinder: %f", volume);
    return 0;
}